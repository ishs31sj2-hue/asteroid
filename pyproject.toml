[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asteroid-reconstruction"
version = "0.1.0"
description = "Convex lightcurve inversion using a Kaasalainen-inspired algorithm"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
]

[project.scripts]
asteroid-reconstruct = "asteroid_reconstruction.cli:main"
